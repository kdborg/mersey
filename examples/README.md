# Examples

`.mersey` files are plain UTF-8 (spec §2.1) — open them in any editor.

## hello.mersey

Demonstrates: class with `private`/`public` and `readonly`, template
literals, non-ASCII code points (each exactly one `char` in a Mersey
string), implicit numeric widening with no string coercion, and exact
`bigdec` arithmetic.
