[package]
name = "mersey_front"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Mersey language frontend: source decoding, lexer (parser and type checker to follow)"

[dependencies]
unicode-ident = "1"
# FxHash for the binder/checker/graph maps — see the `HashMap` alias in lib.rs.
rustc-hash = "2"
unicode-normalization = "0.1"

[lints]
workspace = true
