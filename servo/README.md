# Mersey Servo — Servo integration (Stage B)

> ⚠️ **Mersey Servo (Experimental).** This is an experimental Mersey-engine fork
> of Servo, **not for production use**. It is not affiliated with or endorsed by
> the Servo project; "Servo" here names the upstream engine this fork is built
> on. The build is renamed to **Mersey Servo** (window title, startup banner,
> console notice) so it can't be confused with upstream Servo.

**The fork exists.** `<script type="text/mersey">` in a page is compiled and
executed by the Mersey engine inside Servo — no SpiderMonkey for the Mersey
code, no WASM in the stack — through the same C ABI that `native/host_demo.c`
and `crates/mersey_capi/tests/abi.rs` drive in this repository, and that the
Gecko and Chromium forks drive too. One boundary, four hosts.

Servo is Rust, so the integration is lighter than the C++ forks: the engine is
a **crate** in Servo's build (one `std`, one link — like the Gecko fork's
`gkrust`, unlike Chromium's prebuilt `.so`), and the web bridge is **reflective
Rust→SpiderMonkey**: the host table's `web_*` hooks call the tested
`web/mersey-bridge.js` (five reflective ops — global/get/set/call/construct —
over a handle table) through `JS_CallFunctionName`, exactly as the WASM polyfill
does. The interned **and** wide-string (`web_*_u16`, UTF-16 args/replies, no
JSON) fast paths are wired — the same lever the Ladybird fork got — so the common
get/set/call/new cross without JSON and object arguments stay off the JSON path.
The **direct-DOM tier** (the Ladybird fork's HotMethod dispatch, ported to Rust)
is wired too: a hot interned name (URL ctor/pathname/search, createElement,
appendChild, textContent, Event ctor, dispatchEvent, className, classList,
contains, style, setProperty, getPropertyValue, querySelectorAll, length,
indexed NodeList access — and getRandomValues, TextEncoder.encode /
TextDecoder.decode, and Storage getItem/setItem/removeItem) unwraps its
receiver once via `root_from_object`, caches the boxed native's pointer per
handle (Servo natives never move; the bridge handle table keeps them alive
until `web_release`), and calls the DOM method directly — url 36.2 → 21.7 ms,
cssom 52.5 → 26.3, dom 30.6 → 19.0, events 32.4 → 24.1, query 14.7 → 11.3,
checksums unchanged. `web_bind` (the JIT's compile-time bind ids) is wired too:
the compiled canvas loop dispatches straight to
`CanvasRenderingContext2DMethods::FillRect` — Rust, no JS.

The Servo tree is not *in* this repo (a checkout is large). It is reconstructed
from the overlay on top of pinned upstream and lives beside it at
`browsers/servo`:

```
browsers/servo   git checkout at the pinned upstream revision (see servo/BASELINE)
  components/script/mersey/         the engine module + reflective bridge
  components/script/{lib.rs,Cargo.toml,dom/html/htmlscriptelement.rs}  the hook
```

## What landed

| | |
|---|---|
| `components/script/mersey/mod.rs` | one engine context per script thread (a raw-pointer thread-local — re-entrant by the ABI's thread rule, like `mersey_capi`'s own `MsyContext`). The host table is wired to stdout (`print`), a monotonic clock (`time_ms`), and the web bridge — the interned, wide-string (`web_*_u16`) and typed-binding (`web_bind`) fast paths are all wired, so the common ops cross without JSON and the canvas loop without JS. Same results, verified by matching workload checksums. A JS callback re-enters the engine through `msy_context_invoke_args` (the injected `__merseyInvoke`). |
| `components/script/mersey/bridge.js` | `web/mersey-bridge.js` with the ES import stripped and a `globalThis.__merseyBridge = makeBridge(...)` epilogue. Generated by `refresh-bridge.sh`; do not edit by hand. |
| `components/script/dom/html/htmlscriptelement.rs` | `ScriptType::Mersey`. An **inline** `text/mersey` script runs at prepare time via `crate::mersey::run_mersey_script`, bypassing the classic/module machinery. |
| `components/script/Cargo.toml` | the `mersey_capi` path dependency, default features — the **Cranelift Tier-1 JIT is on**. |
| `vendor/` | *(offline builds only)* the engine's dependency tree, vendored for Servo's offline build: the interpreter's few crates and Cranelift's ~30 (`vendor-deps.sh`). A plain git checkout resolves them from crates.io instead. |

## Building it

The fork is reconstructed on pinned upstream from the overlay and built with a
normal Servo build. From this repo:

```bash
cargo build --release -p mersey_capi        # the engine crate the fork links (jit on)
scripts/fork-overlay.sh bootstrap servo     # clone@rev + apply overlay + build
```

`bootstrap` clones upstream at `servo/BASELINE`'s revision into `browsers/servo`,
applies the overlay, and runs the build hook: `./mach build --release
--media-stack dummy`. The **`--media-stack dummy`** is deliberate — macOS Servo's
GStreamer check wants the official framework (which Mersey's workloads never
need), so this skips it; drop it (or pass `--media-stack gstreamer`) only if you
install the framework. To re-apply the overlay onto an existing checkout, run
`scripts/fork-overlay.sh apply servo`, then `./mach build --release
--media-stack dummy` in `browsers/servo`. On macOS/arm64,
`scripts/build-macos-arm64.sh servo` does the apply + build with the right
defaults.

Servo's own prerequisites still apply (`uv` on `PATH`, `./mach bootstrap` or the
system dev packages). A plain git checkout resolves the engine's crates from
crates.io plus the `mersey_capi` path dependency; an offline, source-vendored
tree is also supported — if the checkout has a `vendor/` dir,
`servo/vendor-deps.sh` populates the engine's crates into it.

The result is `browsers/servo/target/release/servoshell`. Then measure, from
this repo:

```bash
node bench/web/run-native-servo.mjs         # -> bench/web/results.native.servo.json
```

## Notes

- **Ordinary cargo/mach, cross-platform.** Servo is Rust, so none of the
  Chromium fork's arm64 toolchain archaeology applies — the fork is a normal
  `./mach build` once the system libs are present, on macOS and Linux alike.
- **The JIT is on.** `mersey_capi`'s `jit` feature pulls Cranelift; compute runs
  the Tier-1 JIT, which also compiles the web loops once warm. For an offline,
  source-vendored tree, `vendor-deps.sh` copies Cranelift (and the version- and
  target-specific crates `cargo tree` misses) into `vendor/`; a plain git
  checkout needs none of that.
- **http, not `file://`.** The native harness serves over http because Servo
  exposes `localStorage` only on an http origin — which also matches the stock
  runs. The other web workloads are origin-independent.
- **Wide-string fast paths landed.** Wiring the `web_*_u16` hooks (the bridge's
  `*Wide` methods — raw typed replies, UTF-16, no JSON, object args via a refs
  bitmask) cut the bridge-bound workloads sharply: crypto 38.7 → 18.7 ms (−52%),
  url 67.1 → 36.0 (−46%), canvas 27.3 → 18.0 (−34%), dom 35.1 → 31.4 (−11%);
  checksums unchanged. `storage` did not move (2129.9 → 2110.9) — it is bound by
  Servo's own `localStorage`, not the crossing (the direct-Rust `SetItem` path
  confirms it: same numbers). The direct-Rust tier now also covers crypto,
  encoding, storage and — through `web_bind` — the canvas draw loop, so every
  per-iteration op of the twelve workloads reaches Servo's DOM implementation
  in Rust; JS remains only in the one-time reflective setup calls and the
  handle table.
